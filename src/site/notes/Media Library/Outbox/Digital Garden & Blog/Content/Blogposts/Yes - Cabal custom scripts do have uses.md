---
{"dg-publish":true,"dg-path":" Posts/Yes - Cabal custom scripts do have uses.md","dg-permalink":"/posts/yes-cabal-custom-scripts-do-have-uses","permalink":"/posts/yes-cabal-custom-scripts-do-have-uses/","title":"Yes, Cabal custom scripts do have uses","metatags":{"description":"Who would've thunk. (*slow clapping*) Cabal custom scripts everybody"},"tags":["haskell","software-engineering"],"created":"2026-05-12T11:03:10.938-05:00","updated":"2026-05-13T16:18:12.399-05:00","dg-note-properties":{"tags":["haskell","software-engineering"],"title":"Yes, Cabal custom scripts do have uses"}}
---

This will be a quick one. I've been working on implementing replication in my [Redis server](https://github.com/OlaoluwaM/redis-haskell/tree/master). The first step for this, per the [codecrafters](https://codecrafters.io/) challenge that I'm following, is to implement the [`INFO`](https://redis.io/docs/latest/commands/info/) command, which I've done in this [PR](https://github.com/OlaoluwaM/redis-haskell/pull/1) and will be the focus of a future blog post. The framework for the `INFO` command was pretty easy to implement (types ftw). What I found myself spending the most time on was figuring out where the information in the `INFO` response comes from.

For the challenge, the goal was to just implement the `Replication` section with just the `role` field, but I decided to also take a stab at implementing the `Server` section. It was a bit of a tangent. It took a while to hunt down what each field of the section meant and how Redis acquired the field's value. GitHub Copilot was very helpful in helping me navigate and trace things around in the codebase, too. I'll spare you all the mundane details. Suffice it to say, I ended up translating the [Redis code](https://github.com/redis/redis/blob/47c51369eeffd55e1baf20df7955a3dfbe842fc4/src/server.c#L6325) for the server section into this [Haskell module](https://github.com/OlaoluwaM/redis-haskell/blob/9408501f6f9483f79c25ec69e97d4ebe0a448ae7/codecrafters-redis/src/Redis/Commands/Info/Server.hs).

I'm pretty proud of it overall, and through it I found a decent use for [Cabal's custom script mechanism](https://cabal.readthedocs.io/en/stable/cabal-package-description-file.html#more-complex-packages). I use it to collect and generate static information like the git commit hash, git dirty status, and server build ID for the `redis_git_sha1`, `redis_git_dirty`, and `redis_build_id` fields under the "Server" INFO section. Redis does something similar with its [mkreleasehdr.sh script](https://github.com/redis/redis/blob/47c51369eeffd55e1baf20df7955a3dfbe842fc4/src/mkreleasehdr.sh), which generates a header file with release info variables that it then uses to, among other things, seed those server section fields I mentioned in [its `INFO` command implementation](https://github.com/redis/redis/blob/47c51369eeffd55e1baf20df7955a3dfbe842fc4/src/server.c#L6291).

I used Cabal's custom script mechanism to mimic this since I didn't want to have to bend over backwards to try to collect this information at runtime. The setup script creates a `BuildInfo.hs` module that is seeded with constants, which I think is more efficient than whatever runtime attempt at fetching the same data I'd have needed to concoct, which could have required shelling out to git or generating a build ID on each INFO request. I also made it such that the `BuildInfo.hs` module is only regenerated when certain data points change, similar to what the Redis script does. The full setup script can be perused [here](https://github.com/OlaoluwaM/redis-haskell/blob/9408501f6f9483f79c25ec69e97d4ebe0a448ae7/codecrafters-redis/Setup.hs).

Though Cabal custom scripts are [deprecated](https://cabal.readthedocs.io/en/stable/cabal-package-description-file.html#custom-setup-scripts), it was much easier to get set up with them than the [newer recommended setup hooks API](https://cabal.readthedocs.io/en/stable/cabal-package-description-file.html#setup-hooks). There were also more examples on the former to draw inspiration from than the latter, given how new it seems to be. I *will* add a TODO to migrate it over; who knows, I might even get to it someday. But since it is non-blocking, I'll just move on for now.

An alternative approach could have been to use Template Haskell. Template Haskell could do compile-time generation too, but would push metaprogramming into the app code for a small amount of static metadata. It seemed a little overkill for this particular use case.

In any case, that's all for this one folks. Till next time!
